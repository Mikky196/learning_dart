void main(List<String> args) {
  final age = 22;
  print(age);

  // // invalid code
  // age = 23;

  // // The insides of a final variable can be changed. For example, the values in a list declared using the final keyword can be changed. But reassigning a new value entirely is not allowed.
}