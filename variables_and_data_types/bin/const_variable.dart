void main(List<String> args) {
  const name = 'Micheal Adeniran';
  print(name);

  // The below code is invalid since name was already defined as a constant and thus, cannot be modified.
  // name = 'Mikky'
}