void main(List<String> args) {
  String name = 'Micheal'; // Cannot be assigned any value that is not of String data type.
  print(name);

  var age = 22;
  print(age);
  // // Invalid code 
  // age = 'Micheal'; //// This code is invalid as age as assumed the data type of int and thus, cannot be assigned any value other than int values.
}